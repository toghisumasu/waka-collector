# waka-collector 課題一覧表

**最終更新：** 其の五十八時点（2026-07-23）
**凡例：** 優先度は目安。「枯れてから足す」原則に従い、実装着手前に必ず現状把握（Phase 0調査）を挟むこと。

---

## 優先度：高

| # | 課題 | 発見セッション | 内容 | 次の一歩 |
|:--|:--|:--|:--|:--|
| 1 | `ichiza_violations`（一座一句物チェック）の配線バグ | 其の五十八 | `RengasController#create`と`observe_production_hyakuin.rb`両方で、`history`と`candidate`に`:word`キーが一度も設定されておらず、照合が常に空文字同士の比較になっている。テストは通るが本番では実質無効化されている | 其の五十九としてPhase 0調査から。いつから発生していたか、他に同種の配線漏れがないかも確認 |

---

## 優先度：中

| # | 課題 | 発見セッション | 内容 | 次の一歩 |
|:--|:--|:--|:--|:--|
| 2 | 七句去物ルールのShikimokuChecker実装 | 其の五十八 | 実証検証（3作品・零違反）は完了し、実装ドラフトも用意済み。ただし#1のichiza配線バグと同じ`:word`照合方式を踏襲すると同様に無効化されるため、適用は見送り中 | #1の配線修正が完了してから再度実装を検討 |
| 3 | シード辞書の秋偏重・除外後プールの雑優勢 | 其の五十六 | 季節付きシード（雑除く1,556件）の48.8%が秋。must_switch発火時、秋除外後の残りプールも74.3%が「雑」で季節誘導力が弱い。season_label修正だけでは秋偏重（52.5%→53.0%）は解消しなかった | 其の五十七・五十八で語彙拡充を進行中（月・夢・枕・涙・煙・船、いとゆふ・帰雁等）。継続対応 |
| 4 | 記号「◎」の意味未確定 | 其の五十八 | 湯山三吟去嫌一覧に1例のみ出現。誤植の可能性も含め保留 | 実害が出るまで対処不要。参照論文（研究紀要52号）は既に入手済みなので、余裕があれば再照合 |

---

## 優先度：低（観測待ち・実害なし）

| # | 課題 | 発見セッション | 内容 | 次の一歩 |
|:--|:--|:--|:--|:--|
| 5 | `run_observe_and_summarize.sh`のJST/UTC不整合 | 其の五十一 | シェルの`date`はJST、`Time.zone.now`はUTCのため、JST 0:00〜8:59に「ログが見つからない」という誤検知が起きる | 実害が出た際に対処。回避策は把握済み |
| 6 | `observe_production_hyakuin.rb`のrescueスコープ・stderr捕捉（本番側） | 其の四十六〜四十七台 | 観測スクリプト側は対応済みだが、本番側（`RengasController`）の同等対応は未着手 | 継続的な観測で実害が出れば着手 |
| 7 | D-19-5「句数不足（春・秋3句以上必要）」の誤判定 | 其の十九 | `RengaChecker`が前句と付句の2句だけで句数を判定し、孤立した秋2句を見て「3句ない」と誤判定することがある。現時点で実害（誤った句が採用される）は未観測 | 実害が観測されるまで保留 |
| 8 | `sono39_`固定命名プレフィックスの由来 | 其の五十六 | `observe_production_hyakuin.rb`のBATCH_NAME命名規則に古いセッション番号が固定プレフィックスとして残っている（バグではなく仕様）。他のスクリプト・SQLクエリで`sono39`という文字列に依存した処理が残っていないか未確認 | 其の五十七以降のバックログ候補。実害なければ低優先度のまま |
| 9 | 100inプロジェクトの却下理由フィードバック機構との接続 | 其の五十五 | 姉妹プロジェクト100inで、却下理由（指合・去嫌の指摘）がLLMへの次回生成プロンプトにフィードバックされていない。waka-collectorのnext_constraints（事前予防）に対し、事後フィードバックの要否を検討材料としたい | 100in側のPhase0調査結果と合わせて、其の五十五以降で継続検討中 |

---

## 解決済み（参考・直近分のみ）

| # | 課題 | 対応セッション | 結果 |
|:--|:--|:--|:--|
| — | season_label固定バグ（must_switch時も季節指示が秋固定） | 其の五十六 | 修正完了。ただし全体の秋偏重解消には至らず（→ #3へ接続） |
| — | 「月」のBuiDictionary未登録（KIGO_BUIには登録済みの非対称） | 其の五十八 | Phase 1で修正予定（進行中） |

---

*このリストはセッションごとに更新すること。新規課題の発見時はここに追記し、対応完了時は「解決済み」に移動する。*
