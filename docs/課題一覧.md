# waka-collector 課題一覧表

**最終更新：** 其の六十一時点（2026-07-24）
**凡例：** 優先度は目安。「枯れてから足す」原則に従い、実装着手前に必ず現状把握（Phase 0調査）を挟むこと。

---

## 優先度：高

現在該当なし（#1は其の五十九でD-59-1として解決済み。→「解決済み」参照）。

---

## 優先度：中

| # | 課題 | 発見セッション | 内容 | 次の一歩 |
|:--|:--|:--|:--|:--|
| 3 | シード辞書の秋偏重・除外後プールの雑優勢 | 其の五十六 | 季節付きシード（雑除く1,556件）の48.8%が秋。must_switch発火時、秋除外後の残りプールも74.3%が「雑」で季節誘導力が弱い。season_label修正だけでは秋偏重（52.5%→53.0%）は解消しなかった | 其の五十七・五十八で語彙拡充を進行中（月・夢・枕・涙・煙・船、いとゆふ・帰雁等）。其の六十一のsono61_run1で「夢」「月」への収束（七句去物ヒットの93%）を確認、雑の構造化（恋・旅・述懐の部立ヒント新設）の優先度を裏付ける材料として継続対応 |
| 4 | 記号「◎」の意味未確定 | 其の五十八 | 湯山三吟去嫌一覧に1例のみ出現。誤植の可能性も含め保留 | 実害が出るまで対処不要。参照論文（研究紀要52号）は既に入手済みなので、余裕があれば再照合 |

---

## 優先度：低（観測待ち・実害なし）

| # | 課題 | 発見セッション | 内容 | 次の一歩 |
|:--|:--|:--|:--|:--|
| 6 | `observe_production_hyakuin.rb`のrescueスコープ・stderr捕捉（本番側） | 其の四十六〜四十七台 | 観測スクリプト側は対応済みだが、本番側（`RengasController`）の同等対応は未着手 | 継続的な観測で実害が出れば着手 |
| 8 | `sono39_`固定命名プレフィックスの由来 | 其の五十六 | `observe_production_hyakuin.rb`のBATCH_NAME命名規則に古いセッション番号が固定プレフィックスとして残っている（バグではなく仕様）。他のスクリプト・SQLクエリで`sono39`という文字列に依存した処理が残っていないか未確認 | 其の五十七以降のバックログ候補。実害なければ低優先度のまま |
| 9 | 100inプロジェクトの却下理由フィードバック機構との接続 | 其の五十五 | 姉妹プロジェクト100inで、却下理由（指合・去嫌の指摘）がLLMへの次回生成プロンプトにフィードバックされていない。waka-collectorのnext_constraints（事前予防）に対し、事後フィードバックの要否を検討材料としたい | 100in側のPhase0調査結果と合わせて、其の五十五以降で継続検討中 |

---

## 解決済み（参考・直近分のみ）

| # | 課題 | 対応セッション | 結果 |
|:--|:--|:--|:--|
| — | season_label固定バグ（must_switch時も季節指示が秋固定） | 其の五十六 | 修正完了。ただし全体の秋偏重解消には至らず（→ #3へ接続） |
| — | 「月」のBuiDictionary未登録（KIGO_BUIには登録済みの非対称） | 其の五十八 | 対応完了（Phase 1で登録済み） |
| 1 | `ichiza_violations`（一座一句物チェック）の配線バグ | 其の五十九 | D-59-1として本番適用（`c6ac7d5`）。`:word`/`:text`/`:plant_type`を配線し、一座一句物・体用区別・植物異種間句去を有効化。`script/verify_ichiza_taiyo_wiring.rb`で合成検証、`sono60_run1`（100句本番run）で実データ検証も実施。修正前run（sono58以前）に一座一句物語「嵐」の無効な重複4回（本来3件は違反）を確認し、バグの実害を実証。詳細は`docs/observation_analysis_其の五十九.md`参照 |
| 5 | `run_observe_and_summarize.sh`のJST/UTC不整合 | 其の五十九 | 修正完了。`RUN_DATE`の算出を`date -u`に変更し、Rails側（UTCデフォルト）と一致させた |
| 7 | D-19-5「句数不足（春・秋3句以上必要）」の誤判定 | 其の五十九 | 実質クローズ。`RengaChecker`が其の三十八以降どのコードパスからも呼ばれていないdead codeであることを確認済みで、`app/services/renga_checker.rb`にその旨を明記した。クラス自体は削除せず残置（削除要否は保留） |
| 2 | 七句去物ルールのShikimokuChecker実装 | 其の五十八 | D-61-1として本番適用。`ShikimokuChecker#nanaku_gomono_violations`実装（`:text`優先・`:word`フォールバック方式でD-59-1の配線ギャップを継承しない設計）、`verify_shikimoku.rb`試験14追加。本番100句run（`sono61_run1`）でng率54%（従来レンジ31.7%〜40.2%超）を確認したが、内訳分析で違反エントリの63%が七句去物によるもので新ルールの正常な発火と判明（バグではない）。「夢」「月」の2語で七句去物ヒットの約93%を占める偏りも確認し、雑の構造化（#3）の優先度を裏付ける実証データとなった。詳細は`docs/handover_20260724_其の六十一.md`・`docs/architecture_decisions.md` D-61-1参照 |

---

*このリストはセッションごとに更新すること。新規課題の発見時はここに追記し、対応完了時は「解決済み」に移動する。*
