# waka-collector 其の十九 引き継ぎ

**HEAD:** f19f1a0 / **テスト:** 55 pass / 0 fail  
**作業日:** 2026-06-28

## 完了事項

- .gitignore整備（.bak / patch_*.py）
- KIGO_BUI定数追加（季語→部立マッピング）
- BUI_EXAMPLE_WORDS定数追加（部立→具体語）
- kigo_hintメソッド追加（forbidden_buiで絞った季語2語を返す）
- build_full_prompt修正（季語注入・禁止語を具体語化）
- 実動作確認：月・紅葉・鹿・菊・時雨・露が安定出現、生成2〜4秒

## 設計の要点

「前の句と合わせて短歌一首になるような続きを作れ」は意図的設計。変更禁止。
前句＋付句＝短歌31音の構造でLLMに自然な接続を促す。

## 残課題

- ng「句数不足」頻発：チェッカーが2句だけ見て誤判定。実害が出るまで保留
- 語尾崩れ（しらけに等）：頻度観察中
- seedsへのbuiタグ付け：未着手
- 句去違反対処：実観測後に着手

## ゲート

git log --oneline -5
git status
bundle exec ruby script/verify_shikimoku.rb 2>/dev/null | tail -5

## 其の二十：Claude Code導入

### インストール（Mac mini上）

node --version
npm install -g @anthropic-ai/claude-code
claude --version

### 初回起動

cd /Volumes/externalHDD/projects/waka-collector
claude

### 導入理由

其の十九でインデント・行番号ずれ・エスケープ問題により
5分の作業に1時間超を要した。Claude CodeはMac mini上で
直接ファイルを読みstr_replaceで編集するため根絶できる。

### 其の二十でClaude Codeに任せる候補

1. seedsへのbuiタグ付け（build_seed_pool改修）
2. 句数不足ng問題の根本調査（renga_checker.rb）
3. 語尾崩れの頻度測定スクリプト作成
