# 引き継ぎ文書 — 其の二十三（2026-06-30）

## HEAD・テスト件数

- **HEAD:** `ab82529` feat: 独吟百韻検証ハーネス＋句数上限強制対処＋縦書き鑑賞HTML生成（其の二十三）
- **テスト:** 55 pass / 0 fail（セッション開始時確認済み）
- **ブランチ:** main（origin/main と同期済み）

---

## 完了事項

| スクリプト／対処 | 内容 |
|:--|:--|
| `script/dryrun_hyakuin.rb` | 独吟百韻検証ハーネス（A案）新規作成 |
| 句数上限強制対処（D-19-5 本格対処） | must_switch 時の Ruby 側強制切り換え実装。違反率 46%→13% に改善 |
| `script/log_to_html.rb` | 縦書き鑑賞 HTML 生成スクリプト新規作成 |
| `public/hyakuin_test.html` | 其の二十三完走分の鑑賞ページを基準データとして保存 |

---

## 実施内容の詳細

### 1. script/dryrun_hyakuin.rb（独吟百韻検証ハーネス A 案）

Ruby がループを握り、以下のサイクルを 100 句分繰り返す：

```
next_constraints → filter_pool → Ollama（メンタムさん）生成
→ ShikimokuChecker 検証 → 履歴追加
```

- ShikimokuChecker 純粋関数設計の試金石として位置づけ
- 完走ログ: `log/dryrun_final_100.log`（100句・FORCED 13句）

### 2. 句数上限の強制対処（D-19-5 本格対処）

`must_switch` 時に Ruby 側で禁止季・重複句をリジェクトし、強制切り換えを実装。

- **改善前：** 違反率 46%
- **改善後：** 違反率 13%

春 5 句で自発的に季節転換した好例も観測された（正常動作確認）。

### 3. script/log_to_html.rb（縦書き鑑賞 HTML 生成）

- `writing-mode: vertical-rl` による古典様式の縦組み表示
- 季節色分け・折ごと区切り・FORCED 句マーキング・統計サマリー付き
- 生成物 `public/hyakuin_test.html` を今後のトータル評価（違反率・詩的品質）の基準データとして保存

---

## 観測された新規事象

### ドライラン実行の不安定性

- 47 句目・70 句目で原因不明のまま 2 回落ちた（3 回目で完走）
- エラーログを標準ログと分離できていなかったため、原因特定が困難
- **次回より標準化：** ドライラン実行時は stderr を独立ファイルへリダイレクトする

```bash
bundle exec ruby script/dryrun_hyakuin.rb 2>log/dryrun_err.log | tee log/dryrun.log
```

### 句数上限対処の正常動作事例

春 5 句連続後、次句で自発的に夏へ転換した。`must_switch` 判定と `filter_pool` 連携の
正常動作事例として記録しておく。

---

## 其の二十四への着手項目

| 優先 | 課題 | 状態 |
|:--|:--|:--|
| 高 | 既出景物の除外（句去・一座一句物） | 未着手 |
| 中 | dryrun 不安定性の原因調査（47/70句目クラッシュ） | 未着手 |
| 中 | 季語所属 ng 調査（其の二十二持ち越し） | 未着手 |
| 低 | 文語体オプション構想（こそ→已然形 等の試金石含む） | 未着手 |
| 低 | 飛躍促進プロンプト | 未着手 |
| 低 | 字数ドリフト対処 | 未着手 |

---

## 守るべき大原則（再掲）

1. **枯れてから足す** — 実害が観測されてから対処する。観測前の予防的追加は禁止。
2. **骨法は Ruby、即興はメンタムさん** — 式目判定・ループ制御は Ruby、付句生成は LLM。
3. **ShikimokuChecker は純粋関数** — 外部状態・DB 参照を持ち込まない。
4. **正解データは水無瀬三吟** — テストの期待値は水無瀬三吟に準拠する。
5. **D-19-1 変更禁止** — モーラ許容 ±1 設計は意図的設計。厳格化しない。
6. **D-22-1 変更禁止** — `(mora - target_mora).abs <= 1` 条件は変更しない。

---

## ゲートチェックコマンド

```bash
git log --oneline -5
git status
bundle exec ruby script/verify_shikimoku.rb 2>/dev/null | tail -5
```

**期待値:** 55 pass / 0 fail（数は増える）。失敗時は作業禁止。

---

## 環境メモ

- **Mac mini SSH:** `ssh -o ServerAliveInterval=60 unpodisaleicloud.com@100.71.107.6`
- **tmux 推奨:** `tmux new-session -s s24` で切断耐性を確保
- **Claude Code:** `cd /Volumes/externalHDD/projects/waka-collector && claude`
- **DB:** `keiba_user`/`keiba_pass`、host `127.0.0.1`
- **Rails server 確認（教訓）:** 其の二十三で Rails server が落ちていた。
  セッション冒頭で必ず以下を確認すること：
  ```bash
  lsof -i :3000
  ```
  プロセスがなければ `bin/rails server -d` で起動してから作業に入る。
